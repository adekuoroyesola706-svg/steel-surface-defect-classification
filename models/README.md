# Models

Trained model binaries are intentionally not committed by default.

After a validated training run, store model artifacts using Git LFS or an external model registry/storage solution if they are too large for normal Git.

Expected filenames:

- `custom_cnn_final.keras`
- `mobilenetv2_final.keras`
- `resnet50_final.keras`
